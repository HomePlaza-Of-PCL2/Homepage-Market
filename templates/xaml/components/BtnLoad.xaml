<local:MyIconTextButton Style="{StaticResource LoadingButton}"
                        LogoScale="0.85"
                        Logo="{StaticResource UseIcon}"
                        EventData="%s" />