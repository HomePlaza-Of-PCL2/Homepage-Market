<local:MyIconTextButton Style="{StaticResource IssueButton}"
                        LogoScale="0.85"
                        Logo="{StaticResource IssueIcon}"
                        EventData="%s" />